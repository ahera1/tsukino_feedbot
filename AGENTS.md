# Repository Guidelines for Coding Agents

このファイルは、このリポジトリで作業する自動コーディングエージェントおよび
AI支援開発ツール向けの、ベンダーに依存しない開発ガイドです。
人間のコントリビューターも簡潔な開発規約として利用できます。

## プロジェクト概要

Tsukino Feedbotは、RSSフィードから記事を取得し、AIで要約してMastodonへ投稿する
Python 3.11アプリケーションです。

- 利用者向けの機能、設定、実行方法は `README.md` を参照してください。
- 主要な処理フローと設計判断は `FLOWCHART.md` を参照してください。
- 環境変数の定義と設定例は `.env.example` を参照してください。

## 主要ファイル

- `main.py`: CLI、デーモンモード、記事処理の主要ロジック
- `models.py`: フィードと記事のdataclass
- `storage.py`: JSONデータの永続化とクリーンアップ
- `feed_reader.py`: RSSフィードの取得と解析
- `ai_manager.py`: AIサービスの選択とフォールバック
- `ai_base.py`: AIサービス共通処理
- `ai_openrouter.py`, `ai_openai.py`, `ai_ollama.py`: 各AIサービス連携
- `ai_service.py`: 旧インターフェースとの互換レイヤー
- `mastodon_service.py`: Mastodon API連携
- `config.py`: 環境変数の読み込みとアプリケーション設定

## 実装方針

- 現在の小規模で直接的な構成を維持し、不要な抽象化や複雑な設計パターンを追加しないでください。
- 関数は短く単一責任にし、意図が伝わる名前を使用してください。
- データ構造には既存のdataclassを活用してください。
- 明示的な要件がない限り、既存のJSON永続化形式と互換性を維持してください。
- AIサービスの優先順位、リトライ判定、フォールバック動作を変更する場合は、全サービスへの影響を確認してください。
- 例外を黙って破棄しないでください。外部APIやファイル操作の失敗は、秘密情報を含めずに原因を記録してください。
- 新しい本番依存パッケージは、標準ライブラリや既存依存関係で解決できない場合だけ追加してください。
- 依頼範囲外のリファクタリングやフォーマット変更を混在させないでください。

## データと秘密情報の保護

- `.env` の内容、APIキー、アクセストークンを出力またはコミットしないでください。
- 設定例を変更する場合は、実在する認証情報を含めず `.env.example` を編集してください。
- `feeds.json`, `data/`, `logs/` は利用者固有の設定または実行データとして扱ってください。
- 明示的な依頼なしに、利用者データを削除、初期化、上書きしないでください。
- ログ、例外メッセージ、テスト用データに秘密情報を含めないでください。
- Mastodonへの実投稿や外部サービスへの実リクエストが発生し得るコマンドは、明示的に依頼された場合だけ実行してください。

## 検証

検証はDocker環境で行い、変更範囲に応じて次を実行してください。

1. Pythonファイルを変更した場合:

   ```bash
   docker build -t tsukino-feedbot-check .
   docker run --rm --entrypoint python tsukino-feedbot-check -m compileall -q /app
   ```

2. `docker-compose.yml` または環境変数の構成を変更した場合、ローカルに `.env` が存在するとき:

   ```bash
   docker compose config -q
   ```

3. 自動テストを追加した場合は、Docker内で全テストを実行してください。

現在、このリポジトリには自動テストスイートがありません。存在しないテストコマンドを推測して実行しないでください。

通常の検証では、次のコマンドを実行しないでください。外部APIへの接続やMastodonへの投稿が発生する可能性があります。

- `docker compose up`
- `docker compose run --rm feedbot-once`
- `RUN_MODE=once` または `RUN_MODE=daemon` での `main.py` 実行

一時ファイルやテスト用コンテナを作成した場合は、検証後に不要なものを残さないでください。

## ドキュメント

- 機能、設定項目、セットアップ、実行方法を変更した場合は `README.md` を見直してください。
- 処理順序、状態遷移、永続化、クリーンアップ、中断処理を変更した場合は `FLOWCHART.md` を見直してください。
- 開発規約、検証方法、重要な作業上の制約を変更した場合は `AGENTS.md` を更新してください。
- コードから容易に分かる内容を複数の文書へ重複して記載しないでください。

## 完了条件

- 変更が依頼された範囲に収まっていることを確認してください。
- 変更に関連する検証を実行し、その結果を報告してください。
- 実行できなかった検証がある場合は、コマンドと理由を明記してください。
- `README.md` と `FLOWCHART.md` への影響を確認し、更新が不要な場合もその旨を報告してください。
- 一時ファイル、秘密情報、利用者データ、無関係な変更をコミット対象に含めないでください。
